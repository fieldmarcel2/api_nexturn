[project]
name = "api-nexturn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fieldmarcel2", email = "tripathi20t@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "jsonify>=0.5",
    "requests>=2.34.2",
]

[project.scripts]
api-nexturn = "api_nexturn:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
